#include "Headers/ShapeGenerator.h"
#include "Headers/Vertex.h"
#include <glm/glm.hpp>

/*

Vertex ShapeGenerator::CreateShape(std::vector<glm::vec3> positions, std::vector<glm::vec3> color, std::vector<glm::vec3> normals, std::vector<glm::vec2> texture)
{
	Vertex vertex;
	vertex.positions = positions;
	vertex.color = color;
	vertex.normals = normals;
	vertex.texture = texture;

	return vertex;
}

Vertex ShapeGenerator::CreateTriangle()
{
	Vertex vertex;

	vertex.positions.push_back(glm::vec3(0.0f, 0.0f, 0.0f));
	vertex.positions.push_back(glm::vec3(0.5f, 0.0f, 0.0f));
	vertex.positions.push_back(glm::vec3(0.5f, 0.5f, 0.0f));
	

	return vertex;
}
*/
